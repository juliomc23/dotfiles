return {
  "nvim-treesitter/nvim-treesitter-context",
  opts = {
    enable = true,
    max_lines = 3, -- o el número que quieras
    separator = nil, -- 👈 esto elimina la barra horizontal
    mode = "cursor", -- se actualiza cuando mueves el cursor
  },
}
